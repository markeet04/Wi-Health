# ESP-IDF build output + generated sdkconfig
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# SECRETS — the real config with WiFi password + device custom token.
# Never commit; copy from main/config.example.h.
main/config.h
